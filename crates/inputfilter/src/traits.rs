// This module is intentionally left empty after removal of legacy types.
// Legacy types removed: OwnedValidator, RefValidator, FilterFn,
// FilterForSized, FilterForUnsized, and associated result type aliases.
// Use `Field` and `FieldFilter` for validation and filtering.
